============
Installation
============

At the command line::

    $ easy_install django-reports

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-reports
    $ pip install django-reports
